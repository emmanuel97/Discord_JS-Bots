

    //visit a place in the city
    let go = (player, place) => {
            
    }

    //travel to a previously unlocked city
    let travel = (player, city) => {
        
    }

    //try to escape battle
    let escape = (player) => {
        
    }

    //retturn to the city if in the wild zone
    let returnCity = (player) => {
        
    }

    //explore the wild zone of a city
    let explore = (player) => {
        
    }

    //unlock a new city
    let conquer = (player) => {
        
    }

    //teleport to a city in the list
    let teleport = (player, city) => {

    }

    //list all available teleport places
    let listTeleportPlace = (player) => {

    }

    