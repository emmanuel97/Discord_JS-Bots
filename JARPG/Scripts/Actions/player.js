
/*
Class Player:{
const name = ""
const  level = 1
const exp = 0
const life = 0
const energy = 100
const satiety = 0
const agility = 100
const strength = 0
const weigth = 0
const damage = 0
const defense = 0
const mana = 0
const adrenaline = 0
const activeProfessions = []
const god = null
*/
    //get player info
    let profile = (player) => {
        
    }

    //get professions
    let getProfessions = (player) => {
        
    }

    //get actives professions
    let getActiveProfessions = (player) => {
        
    }

    //add a active profession
    let addActiveProfession = (player, profession) => {
        
    }

    //remove a active profession
    let removeActiveProfession = (player, profession) => {
        
    }

    //change a active profession, replacing another if necessary
    let changeActiveProfession = (player, oldProfession = null, newProfession) => {
        
    }

    //get profession data
    let getProfession = (player, profession) => {
        
    }

    //player die a loose a level
    let die = (player) => {

    }

    //player level up a level
    let levelUp = (player) => {

    }

    //get your inventory
    let getInventory = (player) => {

    }

    //player use a consumable item
    let consume = (player) => {
        
    }

    //player drop a item
    let drop = (player) => {
        
    }

    //player equip a item
    let equip = (player, item) => {
        
    }

    //player unequip a item
    let unequip = (player, item) => {
        
    }

    //consume life
    let consumeLife = (player, value) => {
        
    }

    //consume energy
    let consumeEnergy = (player, value) => {
        
    }

    //consume satiety
    let consumeSatiety = (player, value) => {
        
    }

    //consume mana
    let consumeMana = (player, value) => {
        
    }

    //consume adrenaline
    let consumeAdrenaline = (player, value) => {
        
    }

    //recover life
    let recoverLife = (player, value) => {
        
    }

    //recover energy
    let recoverEnergy = (player, value) => {
        
    }

    //recover satiety
    let recoverSatiety = (player, value) => {
        
    }

    //recover mana
    let recoverMana = (player, value) => {
        
    }

    //recover adrenaline
    let recoverAdrenaline = (player, value) => {
        
    }

    //train figthing abilities
    let train = (player) => {

    }

    //add/sub a value to a stat
    let alterStat = (player, stat, value) => {
        
    }

    //give gold to a player
    let giveGold = (player,to, gold) => {
        //if(gold>0)
    }

    //start a trade with a player
    let startTrade = (player,to) => {
        
    }

    //cancel a trade
    let cancelTrade = (player,trade) => {
        
    }

    //user make a offer in a trade
    let addOffer = (player,trade) => {
        
    }

    //user remove a offer in a trade
    let removeOffer = (player,trade, num) => {
        
    }

    //user remove all offer in a trade
    let removeOfferAll = (player,trade) => {
        
    }

    //user accept a offer in a trade
    let acceptOffer = (player,trade) => {
        
    }

    //user cancel a accepted offer in a trade
    let cancelOffer = (player,trade) => {
        
    }

    //list the information of a trade
    let infoTrade = (trade) => {
        
    }
    
    //sucesfully finish a trade
    let concludeTrade = (trade) => {
        
    }

//}